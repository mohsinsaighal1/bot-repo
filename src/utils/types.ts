export type IconProps = {
  size?: number;
  className?: string;
};

export type UserType = {
  id: number;
  first_name: string;
  last_name: string;
  username: string;
  photo_url: string;
  // Add other fields as necessary
};
